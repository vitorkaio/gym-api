type User {
  id: ID!
  username: String!
  perfil: String!
  create_at: String!
  trainings: [Training]
}

input UserInput {
	username: String
	password: String
  perfil: String
  trainings: [ID]
}

input UserFilter {
	id: ID
}
